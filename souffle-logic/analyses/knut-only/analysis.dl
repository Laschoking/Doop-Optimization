#include "../../facts/flow-sensitive-schema.dl"

.comp KnutOnlyTestConfiguration : AbstractConfiguration{

.type Context = MethodInvocation | UniqueContext
.type HContext = symbol
.decl Methods(?method:Method)
Methods(?method) :- ApplicationMethod(?method).
    
}
